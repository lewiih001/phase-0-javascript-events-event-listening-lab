function addingEventListener() {
   // (input.addEventListener.called).to.be.true;
   // binds an event listener in addingEventListener
    let buttons = document.getElementById
    ('button')
{
        buttons.addEventListener('click',()=> alert('i have been clicked'))
    }
}
addingEventListener()